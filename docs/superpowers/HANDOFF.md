# Session Handoff — read me first in a fresh context

**Project:** MTL: Open Île — GTA-like free-roam downtown Montreal. Spec: `docs/superpowers/specs/2026-07-18-montreal-open-world-design.md`.

**State (2026-07-23): M9c3 SAFETY & CLEANUP COMPLETE — AWAITING USER GATE (this gate COMPLETES M9c "Depth & Life" AND all of M9 Graphics Realism II)** (M9c2 gate PASSED, tag `m9c2-life` pushed; plan `docs/superpowers/plans/2026-07-23-m9c3-safety-cleanup.md`, executed inline/controller-led given trivial scope). GAME-SIDE ONLY, no rebuild, 314 tiles, NO visual delta (defensive/cleanup). Shipped: **Lamp colormap safety net** — `props_multimesh.gd::_find_mesh`'s KEEP branch (materials not in `MAT_COLORS`) now re-assigns `props/Textures/colormap.png` when the kept material has no `albedo_texture` (mirrors `car_visual.gd::_polish_materials`; guards the white-car-bug class — a fresh import drops the external-URI colormap → white lamp; the m8c watch-item); `game/tests/props_colormap_test.gd` asserts the lamp material is textured/colored (`PROPS COLORMAP OK`). MAT_COLORS props (trees/bench) take the `if` branch and are provably untouched. **`vnight` consolidation** — `day_night.gd` dropped the redundant local `vnight` (identical to the `night_amount` member = `1.0 - daylight`, assigned earlier in the same `_process`); the 3 volumetric-fog lines now use `night_amount`. Value-identical (night pixel-identical to m9c2). Smoke 20 gates (`PROPS OK` unchanged, `SMOKE OK: 314 tiles`), pytest 100. FINAL whole-branch review (sonnet, scaled to the tiny diff) Ready to Merge, zero Critical/Important (night_amount ordering/value-identity confirmed, colormap net confined to KEEP branch + safe in-place mutation + MAT_COLORS props untouched, sole Aymanbalaa/noreply, no BOM). **CARRY-FORWARDS (Minor, non-blocking):** (1) the m9c3 plan doc wrongly states the hydrant takes the MAT_COLORS branch — it's `Atlas.042` → KEEP branch, but moot (0 hydrants placed + its texture is embedded, not external-URI); (2) `props_colormap_test` ORs textured-ness across surfaces (moot: lamp is single-surface; tighten to per-surface if extended to multi-material props). NEXT: **USER GATE — passing it COMPLETES M9c (9c1+9c2+9c3) and all of M9 Graphics Realism II**; if passed → tag (suggest `m9c3-safety`), push. Then next candidates (needs a fresh brainstorm): pedestrians/traffic AI (M10), missions/gameplay loop, a bolder-awnings retune (AWNING_DEPTH + front valance), or audio polish.

**Prior state (tag `m9c2-life`, pushed — gate PASSED): M9c2 LIFE & FLAVOR COMPLETE** (M9c1 gate PASSED, tag `m9c1-depth` pushed; M9c "Depth & Life" spec `docs/superpowers/specs/2026-07-23-m9c-depth-life-design.md`; plan `docs/superpowers/plans/2026-07-23-m9c2-life-flavor.md`, subagent-driven, per-task reviews clean). WORLD REBUILT (still 314 tiles, no orphans; 96 commercial-bearing tiles modified — awnings are additive). Shipped: **CC0 rigged player** — `game/assets/models/character/character.glb` = Kenney Mini Characters (CC0, license live-verified via WebFetch, `SOURCE.md`; 32 anim clips incl `idle`/`walk`/`sprint`, 7 bones incl `head`); `character_visual.gd` reworked (mirrors `car_visual.gd` load+fallback): loads the model, drives `idle`/`walk` by parent speed (`loop_mode = LOOP_LINEAR`), re-attaches the tuque via `BoneAttachment3D` to the `head` bone, applies a colormap safety net (character.glb refs `Textures/colormap.png` external-URI = SAME white-car-bug class — `_apply_colormap_safety_net` mirrors car_visual `_polish_materials`, runs BEFORE tuque attach so the tuque box isn't tinted), and keeps the procedural blocky char as fallback when the GLB is absent (limb-swing provably unreachable in model mode). Tuque enlarged for the model's chibi head; `idle` clip is subtle (source-asset trait). **Storefront awnings** — a NEW tile bucket `awnings` (awnings CANNOT ride the `buildings` bucket or the facade window-shader would project windows onto them): `pipeline/meshes.py::awning_mesh` emits sloped colored quads on COMMERCIAL (`config.BUILDING_CATEGORIES==commercial`) footprint edges ≥`AWNING_MIN_EDGE 3.0` (poly CCW-oriented → right-perpendicular outward normal; winding regression-tested up+outward; `AWNING_COLORS` are 0-255 ints — 0-1 floats floor to black through `_paint`'s uint8 clip), bucketed in `tiler.py` (added to the assembly cat tuple), routed in `tile_loader.gd` to a flat vertex-colored `StandardMaterial3D` (`vertex_color_use_as_albedo` + `vertex_color_is_srgb`), NO collision. Awnings render as colored bands around commercial storefronts — visible from above / near commercial buildings, subtle in the fixed street poses (like the m9c1 interiors). Smoke gains a 20th gate `AWNINGS OK: 96 awning meshes` → 20 gates. pytest 97→**100** (2 awning + 1 winding-regression test), smoke 20 gates. Screenshots `docs/screenshots/m9c2_*.png` (13). FINAL whole-branch review (fable) Ready to Merge, zero Critical/Important (CC0 license live-confirmed, colormap net correct+ordered, fallback + limb-swing gating, awnings COMMERCIAL-only additive + `awnings` bucket string identical at all 5 wiring points + no collision, deterministic, m9b/m9c1/alpha-packing/`night_amount`/`bhash` untouched, sole Aymanbalaa/noreply). Post-review polish: idle/walk `loop_mode = LOOP_LINEAR` (was incidental replay-on-finish). **CARRY-FORWARDS (all Minor):** (1) `character_model_test.gd` seeds its AABB with the origin (native-dim figure slightly off); (2) `_jitter_pick` uses `math.sin` not the pure-integer `_jitter3` hash (cross-platform last-ulp risk; changing it re-rolls awning colors → needs a rebuild, deferred); (3) awnings appear on party/alley walls + can overlap at reflex corners of concave commercial footprints (plan-accepted v1; street-facing-edge detection is the future refinement); (4) awnings read subtle at street level — bump `AWNING_DEPTH` / add a front valance face for a bolder look (rebuild). Gate PASSED; tag `m9c2-life` pushed.

**Prior state (tag `m9c1-depth`, pushed — gate PASSED): M9c1 RENDERING DEPTH COMPLETE** (M9b gate PASSED, tag `m9b-streets` pushed; M9c "Depth & Life" spec `docs/superpowers/specs/2026-07-23-m9c-depth-life-design.md` splits into 9c1 rendering depth → 9c2 life&flavor [player model + storefront awnings, pipeline rebuild] → 9c3 safety [lamp colormap net + vnight/night_amount consolidation]; 9c1 plan `docs/superpowers/plans/2026-07-23-m9c1-rendering-depth.md`, subagent-driven, per-task reviews clean). GAME-SIDE ONLY, no rebuild, 314 tiles. Shipped: **Fake window interiors** — `building_windows.gdshader` interior-mapping: on commercial (`cat==1`) floors ≥1 inside the `inner` glass, an analytic per-window room box (basis from world-space cell axes + wall normal, NO tangents — `up_ref` guards the degenerate `cross(vec3(0,1,0),nrm)` NaN that would survive a zero blend weight) shades a procedural interior (warm/cool/dim palettes hashed per cell, depth-shaded back wall, contrast 0.7). DAY-GATED (`1-smoothstep(0.45,0.9,night_amount)`) so night is byte-identical by construction; the EMISSION night-window path is untouched. Subtle by design — parallax reads best up-close at oblique angles; downtown poses view glass fairly head-on. **Camera-near grass** — `grass_field.gd` (`static is_grass`: `(g-r)>=0.06 && (b-g)<0.04`, mirrors the terrain shader grass zone; + Node3D renderer): ONE recycled MultiMesh of QuadMesh tufts within `RING 70m` of the active camera, rebuilt only on `STRIDE 3m` cell-cross, `MAX_BLADES 1400`, ~40% hashed-sparse; green cells discovered incrementally by scanning resident terrain tiles once (`_scanned` per terrain-mesh + `_scanned_roots` per tile-root — a root is cached only when NO terrain mesh was distance-skipped, so it stays lossless on partially-in-range tiles); `grass.gdshader` per-instance wind via `MODEL_MATRIX[3]`, smooth root→tip gradient, dims at night. KEY LESSONS (cost real debugging): tiles are ALL resident but visibility-culled (800m) — scan near+visible only; tile transforms are IDENTITY with absolute-baked world vertex coords so `MeshInstance.global_position` is always origin (→ use the world-AABB closest-point distance, not node position); terrain meshes live at `tile_root/world/terrain` (→ recursive `find_children`, matching tile_loader.gd); MultiMesh instances share `NODE_POSITION_WORLD` so wind must key off `MODEL_MATRIX[3]` or all blades sway in lockstep. Smoke gains a 19th gate `GRASS OK: <n> green cells, <m> blades` (asserts green_cells>0; 744 near spawn, blade count is location-dependent — 34 on the paved Vieux-Port spawn plaza, grows as the camera moves into parks). pytest 97 (unaffected — game-side phase), smoke 19 gates. Screenshots `docs/screenshots/m9c1_*.png` (13). FINAL whole-branch review (fable) Ready to Merge, zero Critical/Important (night contract, alpha-packing, `night_amount`, `bhash`, pooling, scan-cache correctness, sole Aymanbalaa/noreply authorship all verified); post-review polish applied (grass skips placement on ray-miss instead of floating at y=0; blade `top` is a smooth gradient). **CARRY-FORWARDS (all Minor, deferred):** (1) `_scan_near_terrain` re-walks far-but-visible tile roots' subtrees every 0.25s (invisible roots short-circuit; cache each root's terrain world-AABB after first walk for O(near) steady state — promote to a shared helper if 9c2 adds terrain-scan consumers); (2) up to ~1400 synchronous raycasts on one cell-cross in fully-green areas (within the ~2ms budget; first knob if park frame-time regresses); (3) interior basis slightly sheared on the narrow band of tilted partially-windowed faces (|ny| 0.25–0.4; `rl.y` uses raw `rd.y` not a true bitangent — imperceptible). Gate PASSED; tag `m9c1-depth` pushed.

**Prior state (tag `m9b-streets`, pushed — gate PASSED): M9b STREET DETAIL COMPLETE** (M9a gate PASSED, tag `m9a-atmosphere` pushed; M9 spec `docs/superpowers/specs/2026-07-23-m9-graphics-realism-2-design.md`; plan `docs/superpowers/plans/2026-07-23-m9b-street-detail.md`, subagent-driven, per-task reviews clean). WORLD REBUILT (still 314 tiles, no orphans; deterministic export — only tiles with new crosswalk/road geometry changed bytes). Shipped: **Crosswalks + stop bars** — pipeline `roadmark_mesh` (pipeline/meshes.py) emits white zebra bars (`CROSSWALK_BARS 4`) + stop bars at clear-interval boundaries that ABUT junctions (way-end boundaries at `SIDEWALK_END_TRIM` are skipped; interior boundaries are junction-caused); rides the existing roadmarks bucket, no game-side change, no new buckets. **Manhole decals** — new `pipeline/decals.py::manhole_spots` (deterministic, sampled along named CAR_SPAWN_CLASSES roads, `MANHOLE_SPACING 57`, `MAX_MANHOLES 600`, cap hit → 512) exported to `city_metadata.json["manholes"]`; new PNG/alpha-composite texture slot (pipeline/textures.py: `fmt` in zip name — legacy JPG keeps bare `{id}_{res}.zip` for cache/lock back-compat; `compose_alpha` builds RGBA from Color+Opacity, writes `game/assets/textures/pbr/manhole_alb.png` — `out_subdir` dropped per plan's simpler fallback; asset `ManholeCover001`, CC0); runtime `game/scripts/decal_pool.gd` = POOLED 32 `Decal` nodes recycled to nearest manholes ≤120 m of the active camera every 0.25 s (light_pool.gd pattern), deterministic yaw via `_hash01`. **Ground-floor doors** — `building_windows.gdshader`: ~1 cell in 5 on residential/default (cat 0/5) floor-0 punches a door through the band (windows/frame/sill/emission zeroed in door cells); commercial (cat 1) storefronts untouched. PLAN BUG fixed in review (commit 314dca6): the plan's `storefront < 0.5` door gate was unsatisfiable (cat 0/5 floor-0 storefront is unconditional) → doors were dead code; dropped the condition. Smoke gains an 18th gate `DECALS OK: 512 manholes` → 18 gates ending `SMOKE OK: 314 tiles`; pytest 92→97. Screenshots `docs/screenshots/m9b_*.png` (13; day gains crosswalks at intersections + residential doors + subtle manholes, night byte-identical to m9a — verified no regression). FINAL whole-branch review (fable) Ready to Merge, zero Critical/Important. **CARRY-FORWARDS (from final review, all Minor):** (1) `AMBIENTCG_API` hardcodes `type=Material` (pipeline/config.py) — a future Decal-typed slot would silently fetch a Material; fast-follow = per-slot `type` field (today the fetch is manual-only, lock pins sha256, and a wrong type fails loudly at Opacity extraction); (2) coplanar mark overlap possible — crosswalk bars and centerline dashes share `h()` height, so on ~50 m intervals a dash can land inside the crosswalk zone (same-color = invisible, yellow-under-white renders deterministically; if speckle ever seen, lift crosswalk `h` +0.005 or clip dashes — pipeline retune); (3) way-end junction approaches get no stop bar by design (only the through-road paints; cheap fast-follow = check `r.points[0]/[-1]` in the junctions frozenset). Committed `road.gdshader.uid` (pre-existing untracked convention gap — siblings all tracked) and generated `decal_pool.gd.uid` alongside docs. Gate PASSED; tag `m9b-streets` pushed.

**Prior state (tag `m9a-atmosphere`, pushed — gate PASSED): M9a LIGHT/SHADOW/ATMOSPHERE COMPLETE** (M8 COMPLETE, tags `m8c-assets` pushed; M9 spec `docs/superpowers/specs/2026-07-23-m9-graphics-realism-2-design.md` — direction delegated by user, phases 9a light/shadow/atmosphere → 9b street detail → 9c depth&life candidates; plan `docs/superpowers/plans/2026-07-23-m9a-light-shadow-atmosphere.md`). Game-side only, no rebuild. Shipped: **Shadows** — Sun `directional_shadow_max_distance 500` + `blend_splits` (was default 100 — shadows now reach the vista; no bias artifacts observed). **Grade** — env `adjustment_contrast 1.05`, `adjustment_saturation 1.08`. **Volumetric night fog** — env params in main.tscn (day state: enabled false/density 0, albedo (0.82,0.86,1), length 200, ambient_inject 0); day_night.gd drives `enabled = vnight > 0.25`, `density = 0.004 * smoothstep(0.25,0.7,vnight)` — KEY LESSON: fog extinction applies to the ENTIRE vista beyond volumetric_fog_length (round 1 at density 0.012 blacked out the whole night skyline + sky); keep density tiny and get halos from per-light volumetric energy instead (pool omnis `light_volumetric_fog_energy 10.0`, car headlight spots 1.5 in car.tscn — car.gd ramp writes light_energy only, verified non-conflicting). Day fully inert (single write site). Stray UTF-8 BOMs stripped from day_night/light_pool scripts (PS5 Set-Content hazard — write BOM-less). Smoke 17 gates, pytest 92. Screenshots `docs/screenshots/m9a_*.png` (13; night gains lamp glow domes + headlight splash, day gains shadow reach + richer grade). 2 tuning rounds. Minor cleanup deferred: `vnight` duplicates `night_amount` (future consolidation).

**Prior state (tag `m8c-assets`): M8c ASSETS & FLAVOR COMPLETE — gate PASSED, M8 COMPLETE** (plan `docs/superpowers/plans/2026-07-23-m8c-assets-flavor.md`, subagent-driven, per-task reviews clean; m8b gate PASSED, tag `m8b-streets` pushed). Shipped: **Metadata props** — `city_metadata.json` gains `trees` (16,498)/`benches` (21)/`hydrants` (0) as `[x,y,z]` like `lamps`; parse recognizes `amenity=bench` + `emergency=fire_hydrant`; point props bbox-guarded at parse (no-op today, guards refetches); baked `props` = traffic signals ONLY (`tree_mesh`/`lamp_mesh`/`MAX_TREES_PER_TILE` deleted); world rebuilt, 314 tiles. **MultiMesh renderer** — `props_multimesh.gd` on `Props` node: per-model-variant MultiMeshes CHUNKED into 256 m cells with `visibility_range_end` 1000 (a global MultiMesh drew trees floating over tiles hidden by the 800 m streaming radius — cells cull with the world); per-instance yaw/scale from position hash; smoke gate 17: `PROPS OK: 16498 trees, 499 lamps, 21 benches, 0 hydrants`. HYDRANTS=0: cached OSM extract predates the hydrant tag in the query — a refetch would unpin the whole world, model committed for a future query change. **CC0 prop models** committed `game/assets/models/props/` (Kenney + Quaternius/Poly Pizza, SOURCE.md; licenses verified live in review). GOTCHA: the Kenney tree/bench GLBs carry `KHR_materials_unlit` + `metallic=1` + pastel colors (trees rendered as cyan crystal) and props are TOY-SCALE native (trees ~1.5 m, lamp 0.6 m; hydrant is mm-vertices under a scale-100 node — `_find_mesh` accumulates node transforms into instance transforms, post-final-review fix) — `props_multimesh.gd` replaces known material names (`leafsGreen`/`woodBark`/`wood`) with shaded naturals and rescales per spec (trees 3.0–4.6×, lamp 7.5×, bench 2.6×). **WHITE CAR FIXED (bug since M6c)** — root cause: default `gltf/embedded_image_handling=1` in the GITIGNORED `.glb.import` discards the external-URI `Textures/colormap.png` reference, so every fresh import produced textureless white cars; fix is a clone-proof idempotent material safety net in `car_visual.gd` (`_polish_materials`: assigns colormap when albedo missing + clearcoat 0.6/metallic 0.25/roughness 0.35); diagnostic `game/tests/car_capture.gd` renders the 6-car lineup (all six distinct: sedan/SUV/taxi/police/hatchback/van). Deferred (surface at gate): AI Montreal decals + crack/manhole decals (need decal/UV infra — own phase if wanted), bus stops. Smoke 17 gates green; pytest 92. Screenshots `docs/screenshots/m8c_*.png` (13). 2 tuning rounds. NEXT: **USER GATE — m8b vs m8c**; if passed, M8 REALISM GRAPHICS PASS IS COMPLETE (tag suggestion m8c-assets) — then candidates: missions/gameplay loop (M9), pedestrians/traffic AI, decal phase, audio polish.

**Prior state (tag `m8b-streets`): M8b STREETS & FACADES COMPLETE — gate PASSED** (plan `docs/superpowers/plans/2026-07-23-m8b-streets-facades.md`, subagent-driven, per-task reviews clean; m8a gate PASSED by user, tag `m8a-light` pushed). WORLD REBUILT (still 314 tiles, no orphans; 223 files changed — terrain-only tiles byte-identical under deterministic export). Shipped: **Per-building variation** — pipeline `_jitter3(osm_id)` bakes brightness 0.86–1.10 + warm/cool ±6% into wall/roof vertex RGB (palette headroom verified, no 255-clipping); facade shader v3 hashes vertex RGB (`bhash`) into a per-building seed → window column width ±(0.85–1.2×), row height ±(0.92–1.12×), lit-window density 26–50% (was fixed 38%), ~15% of storefronts glow cool-white; glass now reflective (ROUGHNESS 0.14, METALLIC 0.55 on inner). SEED CONTRACT: seed = hash of building vertex RGB — changing the palette or `_jitter3` re-rolls all per-building variation (harmless but visible); alpha packing (cat*40+base_y/2) untouched. **Curbs** — sidewalk cross-section verts 0,1 baked `CURB_COLOR (146,143,138)` vs walk `SIDEWALK_COLOR` (stride-4, process=False). **Road/sidewalk/path surface shader** — `road.gdshader` (world-XZ projection, vertex class tint, fbm wear via `wear_strength`; road 0.08/3.8/1.0, sidewalk paving 0.35/2.1/0.2, path ground 0.35/1.15/0.5); `_make_triplanar_material` DELETED. **TANGENT-WARNING FLOOD FIXED (808→0)** — M6a/M7 hypothesis CORRECTED: cause was `uv1_triplanar` StandardMaterial3D itself, NOT the `_rgh.jpg` roughness textures; all three surface materials now shader-based. **2K wall textures** — per-slot `res` key in TEXTURE_SLOTS (brick/stone/concrete 2K, lock/cache res-aware + self-healing). **8a carry-forward fixed**: sun disc gates on `daylight` (no night-sky disc; verified in night pose). Deferred to 8c per plan: crack/manhole decals (needs prop-position infra). Smoke 16 gates green `SMOKE OK: 314 tiles`; pytest 91. Screenshots `docs/screenshots/m8b_*.png` (13; stray `shot_centerline.png` in the Godot user dir keeps sneaking into wildcard copies — deleted, ignore any `m8*_centerline.png`). 2 tuning rounds. NEXT: **USER GATE — m8a vs m8b screenshots**, then 8c Assets & Flavor (real trees via MultiMesh, white-car colormap fix, street props at 499 lamp positions, AI Montreal decals) per spec `docs/superpowers/specs/2026-07-22-m8-graphics-realism-design.md`.

**Prior state (tag `m8a-light`): M8a LIGHT & ATMOSPHERE COMPLETE — direction gate PASSED** (plan `docs/superpowers/plans/2026-07-22-m8a-light-atmosphere.md`, executed via subagent-driven development, per-task reviews clean). Game-side only; world stays 314 tiles, NO rebuild. Shipped: **Sky** — `game/shaders/sky.gdshader` (shader_type sky): fbm clouds, sun disc+halo, stars, twilight wash, driven by `LIGHT0_DIRECTION`; day_night.gd no longer writes sky colors (ProceduralSkyMaterial gone from main.tscn; `night_amount` global contract untouched). CRITICAL TUNING INSIGHT: this project's sun path NEVER crosses the horizon (elevation pins ~6° at night, ~23° at harness dusk t=0.285), so daylight/twilight bands are recentered on sun_h, not zero (`daylight=(sun_h-0.12)*1.9`, `twilight=1-|sun_h-0.35|*4.5`). **Water v2** — analytic wave normals, depth-read shore fade + foam, shore alpha; PLAN BUG FIXED IN REVIEW: opaque materials self-sample `hint_depth_texture` in Forward+, so water is routed to the transparent pass via ALPHA write (`depth_draw_opaque` kept) — NOTE water no longer writes the depth buffer (future SSR/soft-particles near water must know). **Terrain shader** — grass/dirt/rock blend by vertex-color zones (wet `b-g` threshold 0.02–0.06 so documented water-bottom color saturates), slope rock via TRIPLANAR sampling with weights from the GEOMETRIC normal (`cross(dFdx,dFdy)`) — smoothed vertex normals are y-heavy on near-vertical terrain skirts and had caused stretch smears AND a grey rock wash on flat ground (fixed rounds 5–6); anisotropic sampler filtering; new `grass`/`rock` texture slots (Grass001/Rock030, color-only; lock updated on disk — `data/` is gitignored, lock file is NOT tracked, matching all prior slot commits). **Paths** split from sidewalks into own dirt triplanar material (plank-stripe artifact killed). Fog: density 0.00003, aerial_perspective 0.6, sky_affect 0.1. 6 tuning rounds, all screenshot-judged. Smoke 16 gates green `SMOKE OK: 314 tiles`; pytest 87. Screenshots `docs/screenshots/m8a_*.png` (13 keepers; ignore stray `m8a_centerline.png` from spec session). FINAL REVIEW (fable) Ready to Merge; carry into 8b: (1) sky sun-disc gate `smoothstep(-0.06,0.02,sun_h)` not recentered with the other bands — a warm disc can render in the night sky at ~6° elevation (invisible in the 13 poses, free-roam players will find it; fix: gate on daylight or `smoothstep(0.10,0.16,sun_h)`); (2) twilight wash is elevation-symmetric so it also fires mid-morning (~20.5° elevation, matches day_night.gd's own dusk term — inherited quirk, not a bug). Known cosmetic: mild texture striping remains on mid-slope terrain faces (planar grass/dirt projection stretch — full triplanar for grass/dirt is the fix if it bothers, costs 4 more samples); dusk water reads near-black (physically plausible reflection, accepted); blown-white car in driving_day matches m7 baseline (not a regression). NEXT: **USER DIRECTION GATE — present m7 vs m8a screenshots, get pass/fail before ANY 8b planning** (8b Streets & Facades = pipeline rebuild, per spec `docs/superpowers/specs/2026-07-22-m8-graphics-realism-design.md`; then 8c assets/flavor).

**Prior state (tag `m7-polish`): M7 POLISH COMPLETE** (plan `docs/superpowers/plans/2026-07-21-m7-polish.md`, executed via subagent-driven development, per-task reviews all clean). WORLD IS 314 TILES again (bbox filter dropped the 3 edge-signal tiles; the 3 orphaned GLBs were `git rm`ed — the exporter does NOT delete stale tiles, check for orphans after any rebuild that shrinks the set). Shipped: **Audio** — `pipeline/audio_gen.py` deterministically synthesizes `game/assets/audio/{engine,footstep,ambient}.wav` (committed artifacts + force-added `.import` files, the ONLY tracked `.import`s in the repo; regenerate via `.venv/Scripts/python -m pipeline.audio_gen`); engine pitch 0.75+1.5·|speed|/MAX, footstep every 2.4 m grounded, looping −16 dB ambient; loop points set in GDScript (`loop_end = data.size()/2`). **World data** — signals bbox-filtered; `Road.oneway` parsed (`oneway` in yes/true/1/-1), one-way centerlines white (MARK_WHITE) vs two-way yellow; **facade base-height anchor**: wall vertex alpha now packs `category*40 + round(base_y/2)` (roofs stay 255 → cat 6 → concrete branch; bases >78 m clamp, ±1 m quantization), shader decodes and rebases its window grid on `wpos.y - base_y` — uphill storefronts/window rows now hug the ground (verified in `m7_uphill_facade.png`, Avenue du Parc @ 31 m). **Safety** — kill-plane y<-50 respawns controlled body at boot spawn; `_set_flycam()` suspends/resumes controlled-body physics (fixes mid-drive fly-away fall-through + live WASD in fly-cam; smoke calls it directly). **Tuning** — car collision box y 0.55 (bottom at origin), parked spawn drop 0.05 (no hover/sink); `park()` disables CamPivot+Visual processing on all 120 parked cars (perf), `start_driving()` re-enables; headlights ramp `30·smoothstep(0.4,0.55,night)` instead of popping. **Landmarks** get trimesh collision at `_ready` (never ring-freed; `landmark_body_count()`). **Guards** — minimap/hud metadata sub-key guards; `hud.lookup(x,z)` pure + smoke-tested. Smoke sequence now 16 gates: PLAYER/LANDED/CARVIS/CAR/CARS 120/LIGHTS 499/CREDITS/AUDIO/LANDMARKS 5/HUD/ENTER/DRIVE/EXIT/FLYGATE/KILLPLANE/`SMOKE OK: 314 tiles`. pytest 86. Screenshots `docs/screenshots/m7_*.png` (13 keepers incl. uphill_facade + credits overlay). KNOWN COSMETIC: windowed runs spam "shader requires tangents" warnings on road/sidewalk triplanar materials (pre-existing since M6a, ~2 meshes/tile, zero visual impact — fix would be dropping the `_rgh.jpg` roughness textures or baking tangents). REMAINING MANUAL CHECKS: audio audibility (headless can't hear), fly-cam Esc mouse-release, dusk headlight ramp visual (code-reviewed, smoothstep correct). Other open carry-forwards: car spawn cap 120 alphabetical-street bias; terrain 33×33 loop unvectorized (offline-only); kill-plane covers CONTROLLED bodies only (a coasting just-exited car isn't covered — accepted); step accumulator grows unbounded if footstep.wav missing (harmless float). NEXT: **M8 Realism Graphics Pass — 8a ready to EXECUTE, not started.** Spec approved+committed: `docs/superpowers/specs/2026-07-22-m8-graphics-realism-design.md` (3 phases: 8a light/atmosphere → user screenshot direction-gate → 8b streets/facades (pipeline rebuild) → 8c assets/flavor). Plan written+committed: `docs/superpowers/plans/2026-07-22-m8a-light-atmosphere.md` (5 tasks: grass+rock texture slots TDD; sky shader; water v2; terrain shader + paths split; screenshot tuning + gate). Execute via superpowers:subagent-driven-development (user chose "continue later" before picking; subagent-driven was recommended — confirm or just proceed with it). 8a is game-side only, world stays 314 tiles, NO pipeline.build. Task 5 ends at a HARD STOP: present m7-vs-m8a screenshots to the user for the direction pass/fail before any 8b planning. Deferred candidates after M8: missions/gameplay loop, more landmarks, audio polish (car horn, door sounds), pedestrians/traffic AI.

**Prior state (tag `m6c-things`):** **M6 REALISM PASS COMPLETE** (phases a/b/c). 6c: `CityData.signals` parsed from `highway=traffic_signals` (fixture id=53); procedural traffic-light props baked into tiles (pole+arm+3 colored heads, `traffic_light_mesh`, props bucket, no collision) — WORLD IS NOW 317 TILES (signals created 3 new tiles; smoke prints `SMOKE OK: 317 tiles`); 6 Kenney Car Kit CC0 models committed `game/assets/models/cars/car0-5.glb` + shared `Textures/colormap.png` (EXTERNAL DEP — GLBs reference it by relative URI, never move/delete it; SOURCE.md records license, reviewer verified CC0 live); `car_visual.gd` v2 loads model by `color_index % 6` (scale table 1.45-1.55, rotation.y=PI since Kenney faces +Z), procedural blocky car is the automatic fallback, wheel animation procedural-mode only (models static, v1); credits overlay (C toggles, CanvasLayer 10) + repo-root CREDITS.md (OSM ODbL, HRDEM OGL-Canada, ambientCG CC0, Kenney CC0, Godot MIT); `models_test.gd` validates GLBs headless. Smoke sequence now: PLAYER/LANDED/CAR/CARVIS model/CARS 120/LIGHTS 499/CREDITS/ENTER/DRIVE/EXIT/SMOKE OK 317. pytest 79. Screenshots `docs/screenshots/m6c_*.png`. NEXT: **M7 Polish** — audio (engine loop, footsteps, ambient), tuning, and carry-forward burn-down (kill-plane y<-50 respawn, fly-cam hazards, landmark collision, storefront absolute-y anchor, oneway centerlines, collision-box/wheel alignment, parked-car perf, headlight dusk pop, credits overlay untested visually — manual C-toggle check pending like fly-cam Esc; 3 edge-of-bbox signal tiles have floating traffic lights with no terrain patch — bbox-filter signals in M7).

**Prior state (tag `m6b-light`):** M6 phase b complete (Plan 6b). Pipeline: metadata gains `lamps` (499 × [x,y,z]); texture lock entries record `preferred` (fallback-resolved slots now cache-hit; real lock self-heals with one refetch per slot on next real ensure_textures run). Game: sun 2.3×, warm COLOR ambient (source=Color, script-lerped night(0.17,0.19,0.30)→day(0.92,0.90,0.86)→dusk tint — replaced the blue sky-sampled ambient that washed everything), fog 0.00002; `night_amount` exposed on the Sun node via group "sun"; LightPool (24 shadowless OmniLights, energy 9.0, range 20, reassigned 4 Hz to nearest lamps ≤140 m of active camera at night); car headlights (2 shadowed SpotLights, energy 30, on iff driven ∧ night>0.45). DESIGN DECISION: SDFGI dropped after screenshots — it replaces ambient with GI that cannot converge across screenshot settles and keeps invalidating under the moving sun; revisit only with a static-sun mode. ACES NOTE: night light energies must be much higher than intuition (pool 9, headlights 30). Smoke adds `LIGHTS OK: 499 lamps`. pytest 77. Screenshots `docs/screenshots/m6b_*.png` (day is bright/warm/white-greystone; night skyline of lit windows is the money shot; dusk salmon over the port). NEXT: **6c Things** — real CC0 car models (metallic/clearcoat, swap path on car.tscn Visual; re-check 0.15 m curb margin), traffic lights from OSM `highway=traffic_signals` (parse field + metadata + placement), credits screen (OSM ODbL, HRDEM, ambientCG, model credits), + carry-forward triage. New minor carry-forwards: paving texture reads plank-striped at path scale (retune scale/texture in 6c); night foreground still noir-dark away from lights (acceptable; revisit with 6c car paint).

**Prior state (tag `m6a-surfaces`):** M6 realism pass phase a complete (spec: `docs/superpowers/specs/2026-07-19-m6-realism-pass-design.md`, plan 6a executed). Pipeline: CC0 textures fetched from ambientCG (`pipeline/textures.py`, lock `data/textures/textures.lock.json`, committed maps `game/assets/textures/pbr/` — brick=Bricks059, stone=Bricks075A, concrete=Concrete034, roof=Gravel022, asphalt=Road012A, paving=PavingStones128, ground=Ground037/UNUSED for now); building walls carry category in vertex alpha (0 res/1 com/2 church/3 ind/4 civic/5 default; roofs 255); sidewalks with 33° climbable curbs + junction-aware clearance (`_clear_intervals`, junction = rounded point shared by ≥2 vehicle ways); dashed centerlines + arterial edge lines; pedestrian classes split into `paths` geometry (paving material). Game: facade shader v2 (triplanar walls by category, window frames/insets/sills, storefront band floor 0 with night glow 0.55, roof-face branch), triplanar StandardMaterial3D for roads (brighten 3.2)/sidewalks+paths (0.8, 1.8); NO normal maps (tiles lack tangents — streak artifacts, hence removed); terrain reverted to vertex colors (Ground037 was too green + tangent streaks — retexture properly in 6b/6c). pytest 76; smoke unchanged full OK. LESSONS: normal_texture on tangent-less meshes = streaky garbage + warning flood; judge every texture slot by Reading the jpg (brick/stone arrived swapped); sidewalk winding — outward columns flip the road-ribbon face pattern. NEXT: **6b Light** (SDFGI, sun/exposure/fog/grade — the current look is murky blue precisely because lighting is untouched; storefront blowout at night to re-tune under new exposure; streetlight pool from metadata `lamps` (NOT yet emitted — add to export), car headlights). Then 6c Things (real CC0 car models, traffic lights from OSM signals — parse not yet done, credits screen). Carry-forwards from M5 all still open (landmark collision, kill-plane, fly-cam hazards, HUD test, minimap guards, perf items) plus: sidewalk/path overlaps at junction gaps read okay but curbs stop abruptly; commercial facades read very blue in flat light (re-judge after 6b); storefront/floor-0 band is anchored to ABSOLUTE world y (buildings uphill get no storefronts and world-aligned window rows — needs a per-building base-height vertex attribute + rebuild, 6b/6c); one-way streets get two-way yellow centerlines (no oneway field in parse — add with 6c signals parse); textures are pre-fetched and committed, rebuilds never touch the network (ensure_textures is manual, not in pipeline.build); FIRST PIPELINE TOUCH OF 6B: fix ensure_textures fallback-cache contract (API-fallback-resolved slots can never cache-hit — record preferred in lock entry) + test; re-check 0.15 m car-vs-curb margin when 6c swaps car models.

**Prior state (tag `m5-driving`):** Milestones 1–5 complete. M5 "Behind the wheel": pipeline emits `car_spawns` (120 poses sampled every 90 m along named downtown roads within 700 m of origin, right-edge offset, yaw = atan2(-dx,-dz), y from heightmap + 0.05; `pipeline/cars.py`); game spawns them as parked `Car`s (CharacterBody3D arcade physics — signed speed, speed-scaled steering, handbrake=Space; procedural blocky visual with palette-by-index, spinning/steering wheels, emissive head/taillights; chase CamPivot/SpringArm/CarCamera rig). DESIGN DECISION (recorded in plan): procedural car + custom arcade physics instead of spec's Easy Vehicle Physics + Kenney kit — no downloads, deterministic, license-clean; swap path touches only car.tscn/car.gd. CRITICAL INVARIANT: parked cars are physics-frozen (`park()`) because tile collision only exists in the 280 m ring around the ACTIVE camera — a simulated car outside it falls through the world; cars refreeze after `stop_driving()` once slow+grounded. Enter/exit: E near a car (3.5 m) via main.gd state machine (`_enter_car/_exit_car`, reentrancy-guarded — screenshot harness calls them directly), player hidden + physics + capsule disabled while driving, exit places player at car's left (−basis.x·2.2); F fly-toggle works against whichever game camera is active; HUD prompt + km/h speedometer; minimap/street HUD track the car for free (they follow the active camera). Smoke prints PLAYER OK / LANDED OK y=0.0 / CAR OK / CARS OK: 120 / ENTER OK / DRIVE OK v=3.9 / EXIT OK / SMOKE OK: 314 tiles; pytest 58. Screenshots `docs/screenshots/m5_driving_*.png`. ENV NOTES: fresh clone must run `--import` once before headless scripts using class_name types; headless frame:physics ≈ 2:1 — never fixed-frame physics waits in tests, poll conditions (smoke drive poll requires speed AND distance — at ACCEL 9.0 a car first passing 3 m/s has only moved ~0.55 m). Next: M6 "Polish" (audio, tuning, credits/attribution screen: OSM ODbL + Montreal CC-BY + asset credits). Open carry-forwards: landmark collision missing (cars/players pass through hero landmarks), HUD has no unit test, fly-cam Esc manual test, terrain loop perf, minimap.gd guards, enter/exit guard branches untested, player WASD still live in fly-cam mode, driven car falls through the world if fly-cam is toggled mid-drive and flown away (ring follows the fly cam; car input/physics not camera-gated), no kill-plane/respawn (driving off a quay into the river falls forever — y<-50 respawn candidate), parked-car collision box bottom 0.2 m below wheel contact (0.4 m hover parked / 0.2 m sink driven — M6 tuning), 120 parked cars run SpringArm+visual _process every frame (M6: disable CamPivot processing while not current), car spawn cap 120 hit with alphabetical street bias.

**Prior state (tag `m3.5-wow`):** Milestones 1–3.5 complete. M3.5 WOW pass added: emissive window shader (world-space grid, `night_amount` shader global set by day_night.gd via RenderingServer), ProceduralSky + SSAO + glow + ACES, 16,498 street trees + 499 lamps from OSM nodes as per-tile "props" geometry (300 trees/tile cap), animated water shader, roof caps + residential gables, water/green split into named tile geometries, materials routed by geometry name in tile_loader. Screenshots `docs/screenshots/m35_*.png` (7 poses incl. dusk + night; all reviewed visually over 4 tuning iterations — key fixes: shaders must linearize sRGB vertex COLOR with pow(2.2); emission gated by smoothstep(0.45,0.9,night_amount) so day facades stay matte; glancing-angle emission fade). CROSS-TASK SEAM BUG caught at final review: export.py's landmark-exclusion CityData rebuild silently dropped the new trees/lamps fields — fixed with export-level regression test (test_export_preserves_props). Remote: github.com/Aymanbalaa/grand-theft-poutine (public). Earlier state below still applies.

**Prior state (tag `m3-montreal`):** Milestones 1–3 complete. The city is now recognizably Montreal: stylized palette baked as vertex colors (brick residential, glass commercial, greystone churches), St. Lawrence river + relation-based parks via multipolygon support, Mont Royal terrain from a REAL HRDEM heightmap (NRCan STAC fetch worked; synthetic fallback exists), five procedural hero landmarks (coords OSM-verified except Five Roses — no OSM name match, brief default kept) (PVM, Notre-Dame, Biosphère, Habitat 67, Five Roses), day/night cycle (T fast-forwards), minimap HUD with camera marker. 314 tiles, 45 pytest green, smoke prints `SMOKE OK: 314 tiles`. Milestone screenshots at `docs/screenshots/m3_*.png` (reviewed visually). Plan 2 executed via superpowers:subagent-driven-development; ledger at `.superpowers/sdd/progress.md` (gitignored).

**Next up — Plan 3 "On foot" (M4), to be written with superpowers:writing-plans:**
- Third-person CharacterBody3D controller (walk/sprint/jump), collision-aware follow camera (Quaternius/Mixamo character)
- Collisions for buildings/terrain (tiles currently have no collision shapes — generate trimesh collision in pipeline or Godot import settings). NOTE: building meshes are NOT watertight since M3.5 (roof cap slabs are open) — convex decomposition/`is_volume`-dependent approaches will need the wall solids, not the concatenated mesh
- Street-name + district HUD (point-in-polygon vs metadata) — FIRST dedupe `streets` metadata (7,357 per-way fragments, ~2 MB; dedupe by name / split per district) and consume or drop the unused `spawn` field
- Spawn on foot downtown instead of fly cam (keep fly cam as debug toggle)

**Carry-forward review findings (fix when touching the relevant code):**
- Fly cam Esc mouse-release never manually tested; tile visibility toggling has no automated test
- minimap.gd: metadata sub-key indexing unguarded, silent failure paths (plan-mandated simplicity)
- terrain_tile_mesh: 33×33 Python loop per tile unvectorized (offline-only cost); water/green terrain coloring path untested
- geo constants duplicated in terrain.py (`110574.0`/`111320.0` vs geo.py canonical)
- Final whole-branch review for Plan 2: see ledger FINAL entry for any accepted-risk items
- minimap roads use a single light color for legibility (deviation from plan's per-class colors, chosen at screenshot review)

**Hard constraints (user-mandated):**
- Commits authored ONLY by Aymanbalaa via repo-local git config (noreply email) — NO Co-Authored-By/AI trailers ever. History was rewritten once to scrub a gmail leak; don't reintroduce. KNOWN RISK: one subagent passed `--author` with the gmail address (caught + amended before it left the machine) — every subagent dispatch must explicitly forbid `--author` and git-config changes.
- Commit small and frequent. Cheaper-model subagents for mechanical work — orchestrate, review everything.

**Environment gotchas:**
- `tools/godot/godot.exe` is the MAIN Godot 4.5 binary; `tools/godot/godot_console.exe` is the console wrapper — USE THE CONSOLE WRAPPER from PowerShell, the GUI exe detaches and returns no output. Headless smoke: `tools/godot/godot_console.exe --headless --path game --script res://tests/smoke_test.gd` → expect "SMOKE OK: 317 tiles". After regenerating world tiles run `--headless --path game --import` first (minutes).
- Screenshots: `tools/godot/godot_console.exe --path game --script res://tests/screenshot.gd` (windowed, needs GPU) → PNGs in `%APPDATA%/Godot/app_userdata/MTL Open Ile/`. ALWAYS look at them (Read tool) before claiming visual success. 5 poses: overview/street/oldport/mountain/biosphere.
- Python: `.venv/Scripts/python -m pytest pipeline -q`; full rebuild `.venv/Scripts/python -m pipeline.build` (OSM + heightmap both cached in `data/`; delete caches ONLY if the query/source changes).
- Overpass mirrors flaky (rotation built in); HRDEM via NRCan STAC can fail → synthetic fallback auto-engages (check console line "heightmap saved (hrdem|synthetic)").
- Heightmap cache pinned: heightmap.npy sha256=76cabf2012331723…, heightmap_meta.json sha256=6ef96faad06ce025… — if data/ is ever lost, a fresh HRDEM fetch may differ; diff new hashes before rebuilding the committed world.
